language: csharp
sudo: required
dist: trusty

matrix:
  include:
    - dotnet: 2.2
      mono: none
      os: linux
    - dotnet: 2.2
      mono: none
      os: osx
    - mono: latest
      os: linux
    - mono: latest
      os: osx

script:
  - ls
